# 불사조 · [Project C] AI 기반 고객 리뷰 감정 분석 대시보드

## 결과보고서

| 항목 | 내용 |
|---|---|
| 조 이름 | 불사조 |
| 작성일 | 2026. 8. 21. |
| 팀 구성 | 남병광, 이혜경, 황준식 (3명, 가나다순) — 조장: 이혜경 |
| 과제 주제 | AI 기반 고객 리뷰 감정 분석 대시보드 |
| 리뷰 주제 · 타겟 | **[AI 도구 역할]** KDT AI 분야 스타강사(AI 튜터링 프롬프트) / **[데이터 리뷰 주제·타겟 페르소나]** 쿠팡 온라인셀러(1인 창업자, 판매제품: 욕실 틈새 청소솔) |
| 사용한 AI 도구 | Claude |

---

## 목차

1. 프로젝트 개요 및 추진배경
2. 팀 구성 및 역할 배분
3. 과제 수행 방법 (페르소나·도구·워크플로우·프롬프트 설계)
4. 구현 내용 (설치·환경 세팅, 폴더 구조, DB 설계, CLI 명령어)
5. 실행 결과 및 산출물
6. 예외 처리 및 한계점
7. 최종 검증표
8. 팀원 소감
9. 부록 — 사용 AI 도구 및 프롬프트

---

## 1. 프로젝트 개요 및 추진배경

고객 리뷰는 제품과 서비스의 품질을 가늠하는 가장 직접적인 지표다. 그러나 수백에서 수천 건에 달하는 리뷰를 사람이 일일이 읽고 분석하기란 현실적으로 어렵다. 다수의 리뷰를 사람이 읽으려면 며칠이 걸리지만, AI를 활용하면 몇 분 안에 감정을 자동 분류하고 핵심 키워드와 트렌드를 빠르게 파악할 수 있다.

다만 단순히 긍정/부정을 분류하는 데 그친다면 실제 의사결정에 쓰기는 어렵다. 시간에 따른 감정 변화 추이, 반복적으로 등장하는 불만·칭찬 키워드, 별점과 감정의 상관관계까지 함께 봐야 비로소 '다음에 무엇을 개선해야 하는가'라는 실무적 질문에 답할 수 있다.

이에 본 팀은 리뷰 데이터를 수집하고 AI로 감정을 분석한 뒤, 대시보드 형태의 시각화 리포트를 생성하는 CLI 기반 서비스를 구축하였다. 단순 분류를 넘어 시간별 감정 변화, 주요 불만/칭찬 키워드 추출, 별점-감정 상관관계 등 비즈니스 인사이트로 이어지는 결과물을 목표로 하였으며, 실무형 페르소나(1인 창업자·소상공인)를 타겟으로 설정해 실제 활용 가치를 검증하였다.

### 과제 목표

- CSV/Excel 형태의 리뷰 데이터를 수집·정제하고, raw/clean 저장소를 분리 관리하는 파이프라인을 설계·구현
- AI API를 호출하여 리뷰 텍스트의 감정(긍정/부정/중립)과 신뢰도를 분석하고, 결과를 구조화하여 저장
- 조건별(기간·감정·제품) 리뷰를 종합하여 핵심 키워드·요약·개선 제안을 AI로 추출
- 감정 분석 결과를 집계하고 matplotlib으로 감정 분포·시간별 추이·별점-감정 상관관계 등 다양한 차트를 생성
- 분석 결과를 비즈니스 관점에서 해석하고, 실제 활용 가능한 인사이트를 도출하여 리포트로 제공

### 리뷰 주제 및 타겟 페르소나

| 항목 | 내용 |
|---|---|
| 리뷰 주제 | 쿠팡 판매 욕실 틈새 청소솔 구매 리뷰 (샘플 데이터 40건 확보) |
| 페르소나 특징 | 쿠팡에서 '욕실 틈새 청소솔'을 판매하는 1인 온라인 셀러로, 별도의 마케팅·데이터 분석 인력 없이 상품 등록부터 고객 응대까지 혼자 담당한다. 세정력·내구성(솔모 빠짐, 손잡이 파손 등)·배송 상태 등 리뷰에 나타나는 반응을 빠르게 파악해 상품 개선과 상세페이지 문구 수정 여부를 판단하는 것이 중요하다. |
| 니즈 | 리뷰의 감정·평점 추이와 반복되는 불만 키워드(세정력 부족, 솔모 빠짐, 배송 파손·지연 등)를 파악해 상품 개선, 상세페이지 문구 수정, 고객센터(CS) 대응에 반영하고 싶음 |

> 참고: 샘플 데이터는 실제 정제(clean) 로직 검증을 위해 정상 리뷰뿐 아니라 빈 리뷰·짧은 리뷰·날짜 형식 불일치·완전 중복 리뷰 등 의도적 결함 데이터를 함께 포함했으며, 부정 리뷰 급증 알림 기능 검증을 위해 특정 기간(1/22~1/28)에 부정 리뷰가 몰리도록 설계하였다.

---

## 2. 팀 구성 및 역할 배분

| 구분 | 담당 업무 | 주담당자 |
|---|---|---|
| 조장 (역할 1개 겸임) | 코디세이 팀과제 화면 운영 총괄 · 팀미션 시작/팀원 초대 · 승인 요청 · 최종 결과물 검수 · 전문가 1인 평가 신청 · 피드백 공유 및 반영 (+ A 겸임) | 이혜경 (A 겸임) |
| A 문서작업 | README·기획서 작성, 워크플로우/DB 구조 설명 작성, GitHub 저장소 관리 및 최종 업로드 | 이혜경 |
| B 기능 구현 | 기능요구사항 기반 CLI 애플리케이션 구현 (데이터 수집/정제 — 남병광, AI 감정분석·키워드/요약 추출 — 이혜경, 대시보드 시각화 및 리포트/내보내기 — 황준식), 작업과정 이미지 증빙자료 수집 | 황준식 (주담당) / 세부: 남병광·이혜경·황준식 |
| C 테스트·검수 | 기능요구사항 충족 여부 테스트, 샘플 데이터 검증 및 예외 케이스 확인, 버그 리포트 및 개선 요청, 최종 결과물 검수 | 남병광 |

### 업무 진행 순서

1. 기획 및 설계 — 요구사항 분석, 페르소나·리뷰 주제 확정, DB 스키마 합의, 워크플로우 설계
2. 저장소 생성 및 환경 구성 — GitHub 저장소 생성, 폴더/모듈 구조 설계, config.json 및 샘플 데이터 준비
3. 시스템 구현 — CLI(argparse) 설계, 데이터 수집/정제, AI 감정분석·키워드 추출, 조회/통계, 대시보드·리포트·내보내기 기능 구현
4. 테스트 및 개선(반복) — 기능요구사항 충족 여부 검증, 예외 처리 확인, 버그 수정, 결과 캡처
5. README 및 보고서 작성 — 프로젝트 소개, 팀 역할, 기능 설명, DB 스키마, 실행 방법, 결과 캡처 정리
6. GitHub 과제 제출 및 전문가 평가 신청

---

## 3. 과제 수행 방법

### 3-1) 활용 도구

| 구분 | 도구 |
|---|---|
| 개발 언어 / 환경 | Python 3.10 이상 |
| CLI 구현 | argparse (서브커맨드 기반: import, clean, analyze, extract, list, show, stats, dashboard, export) |
| 데이터 저장 | SQLite (raw / clean 분리 저장) |
| AI 감정분석 · 키워드추출 | OpenAI 공식 SDK (`gpt-4o-mini`, JSON 강제 응답 모드) |
| 시각화 | matplotlib (한글 폰트 적용, PNG 저장) |
| 버전관리 및 제출 | GitHub |

### 3-2) 시스템 워크플로우

전체 프로세스는 **데이터 수집 → 정제/클린 → AI 감정분석 → 조회/통계/AI 추출 → 대시보드/리포트/엑셀**의 흐름으로 구성되며, `raw_reviews`와 `clean_reviews`를 분리 저장해 원본을 보존한다.

```
[CSV/Excel 파일]
      ↓ import
[raw_reviews]  ← 원본 그대로 보존
      ↓ clean (필수필드 검증, 정규화, 별점/날짜 검증, 짧은리뷰 필터링, 중복 skip/upsert)
[clean_reviews]
      ↓ analyze (AI 감정 분석: 긍정/부정/중립 + 신뢰도)
[clean_reviews (sentiment 컬럼 갱신)]
      ↓ extract (여러 리뷰 종합 → 키워드/요약/개선제안)
[extractions]
      ↓ stats / dashboard (통계 집계, 부정 리뷰 급증 알림, 차트/리포트 생성)
[output/charts/*.png, output/reports/*.txt,*.md]
      ↓ export
[output/export_*.csv, *.jsonl, *.xlsx]
```

### 3-3) AI 감정분석·키워드추출 프롬프트 설계

```
당신은 쿠팡 온라인셀러(1인 창업자)를 위한 고객 리뷰 분석 전문가입니다.

다음은 판매 상품 '욕실 틈새 청소솔'에 대한 고객 리뷰입니다. 리뷰를 분석해 감정을
긍정/부정/중립 중 하나로 분류하고 신뢰도(0.0~1.0)를 산정하세요.

작성 조건:
1. 감정은 반드시 긍정/부정/중립 중 하나로만 분류합니다.
2. 리뷰에 나타난 핵심 불만/칭찬 키워드를 함께 추출합니다.
3. 확인되지 않은 내용을 추측하여 덧붙이지 않습니다.
4. 리뷰 원문의 핵심 사실만 사용합니다.

반드시 JSON 형식으로만 응답하며, 다른 설명은 추가하지 않습니다.
```

키워드/요약 추출(extract) 시에는 여러 리뷰를 하나의 프롬프트에 번호를 매겨 종합적으로 전달하고, 긍정/부정 키워드·전체 요약·개선 제안을 함께 요청하도록 설계하였다.

---

## 4. 구현 내용

### 4-1) 환경 세팅

- **Python 3.10 이상** 설치 (https://www.python.org/downloads/, "Add Python to PATH" 체크)
- **Git** 설치 (https://git-scm.com/downloads) — 버전 관리 및 GitHub 연동용
- **SQLite** — 별도 설치 불필요. Python 표준 라이브러리(`sqlite3`)에 기본 내장
- **의존 패키지 설치**
  ```bash
  pip install -r requirements.txt
  ```

| 패키지 | 용도 |
|---|---|
| `pandas` | CSV/Excel 파일 읽기 및 데이터프레임 처리 |
| `openpyxl` | Excel(.xlsx) 파일 읽기/쓰기 |
| `matplotlib` | 대시보드 차트 생성 |
| `openai` | AI 감정 분석 및 키워드/요약 추출 API 호출 |

- **API 키 환경변수 설정**: API 키는 코드나 설정 파일에 직접 작성하지 않고 환경변수로 관리한다.
  ```powershell
  $env:OPENAI_API_KEY = "sk-proj-본인의_실제_키"
  ```
  `config.json`에는 실제 키 값이 아니라 `"api_key_env": "OPENAI_API_KEY"`처럼 환경변수 이름만 지정되어 있다.

### 4-2) 프로젝트 폴더 구조

```
review-dashboard/
├── main.py                  # CLI 진입점 (argparse 서브커맨드 정의 및 라우팅)
├── config.json               # 설정 파일 (API 설정, 저장소 경로, 정제/알림 기준값 등)
├── requirements.txt          # 의존 패키지 목록
├── modules/
│   ├── storage.py            # SQLite 연결, 테이블 초기화, list/show 조회 함수
│   ├── data_loader.py        # 파일 로드(import), 정제(clean), 내보내기(export)
│   ├── ai_service.py         # AI 감정 분석(analyze), 키워드/요약 추출(extract), 로거 설정
│   ├── analytics.py          # 통계 집계(stats), 부정 리뷰 급증 알림
│   ├── visualize.py          # matplotlib 차트 3종 생성 (한글 폰트 적용)
│   └── report.py             # 종합 리포트 생성(TXT/MD 저장)
├── data/
│   ├── sample_reviews.csv    # 테스트용 샘플 리뷰 데이터 (40건, 욕실 틈새 청소솔 리뷰)
│   └── reviews.db            # SQLite DB (실행 시 자동 생성)
├── logs/app.log               # INFO/WARNING/ERROR 로그 기록
├── output/
│   ├── charts/                 # 대시보드 PNG 차트 3종
│   ├── reports/                # 리포트 TXT/MD 파일
│   └── export_*.*              # 내보내기 결과 (CSV/JSONL/Excel)
└── screenshots/               # 구현 결과 캡처 이미지 모음
```

최소 4개 이상 모듈 분리 요구사항에 따라, 총 **6개 모듈**(storage, data_loader, ai_service, analytics, visualize, report)로 기능을 분리하여 구현하였다.

### 4-3) DB 구조

원본 데이터와 정제된 데이터를 **분리 저장**하여, 정제 로직에 문제가 생기더라도 원본을 보존하고 재처리할 수 있도록 설계하였다.

| 테이블 | 설명 |
|---|---|
| `raw_reviews` | import 단계에서 원본 그대로 저장 (검증 전) |
| `clean_reviews` | 정제/검증 완료된 데이터 + AI 감정분석 결과(sentiment, sentiment_score) 저장, dedup_key로 중복 관리 |
| `extractions` | extract 단계에서 생성되는 키워드/요약/개선제안 종합 결과 저장 |

### 4-4) 9개 CLI 명령어

| 명령어 | 기능 | 주요 옵션 |
|---|---|---|
| `import` | CSV/Excel 파일을 raw 저장소로 가져오기 | `--file` |
| `clean` | raw 데이터를 정제하여 clean 저장소로 저장 | `--mode skip\|upsert` |
| `analyze` | AI 감정 분석 실행 | `--all`, `--id`, `--unanalyzed`, `--limit` |
| `extract` | 조건별 리뷰 종합 → AI 키워드/요약/개선제안 추출 | `--sentiment`, `--date-from`, `--date-to`, `--product` |
| `list` | 리뷰 목록 조회 (필터+페이지네이션+정렬) | `--sentiment`, `--rating`, `--page`, `--size`, `--sort-by`, `--order` |
| `show` | 특정 리뷰 상세 조회 | `--id` |
| `stats` | 전체 통계 요약 + 부정 리뷰 급증 알림 | (없음) |
| `dashboard` | 차트 3종 + 종합 리포트 생성 (TXT/MD) | (없음) |
| `export` | 데이터 내보내기 | `--format csv\|jsonl\|excel`, `--sentiment`, `--rating-min` |

**전체 파이프라인 실행 예시**
```bash
python main.py import --file data/sample_reviews.csv
python main.py clean
python main.py analyze --unanalyzed
python main.py extract --sentiment negative
python main.py list --sentiment negative --page 1 --size 5
python main.py show --id 1
python main.py stats
python main.py dashboard
python main.py export --format csv
```

---

## 5. 실행 결과 및 산출물

`python main.py stats` 및 `python main.py dashboard` 실행 결과, 총 36건의 clean 데이터(긍정 18건 50.0% / 중립 5건 13.9% / 부정 13건 36.1%)에 대한 감정 분석이 100% 완료되었으며, 아래와 같은 차트와 리포트가 생성되었다.

### 5-1) 감정 분포 (파이 차트)

![감정 분포](images/chart_sentiment.png)

### 5-2) 별점별 감정 분포 (누적 막대 그래프)

![별점별 감정 분포](images/chart_rating.png)

별점이 낮을수록(1~2점) 부정 감정 비중이 높고, 별점이 높을수록(4~5점) 긍정 감정 비중이 높게 나타나 별점과 AI 감정 분석 결과가 대체로 일관된 상관관계를 보였다.

### 5-3) 시간별 감정 추이 (선 그래프)

![시간별 감정 추이](images/chart_trend.png)

샘플 데이터 설계상 2024-01-22~01-28 구간에 부정 리뷰가 몰리도록 하였으며, 실제로 이 구간에서 부정(빨강) 라인이 상승하는 패턴이 확인되었다. 이는 보너스 과제인 "부정 리뷰 급증 알림" 기능의 정상 동작을 시각적으로도 뒷받침한다.

### 5-4) 종합 리포트 (콘솔 출력 및 TXT/MD 파일 저장)

![종합 리포트](images/report_content.png)

리포트에는 핵심 지표(총 리뷰 수, 분석 완료율, 긍정 비율, 평균 별점, 평균 감정 점수), TOP 5 긍정/부정 키워드, AI 인사이트 요약, 개선 제안, **부정 리뷰 급증 알림**(최근 7일간 부정 리뷰 비율 40.0%, 직전 7일 대비 15.0%p 급증)이 모두 포함되어 있으며, 콘솔 출력과 동시에 `output/reports/`에 TXT/MD 파일로 저장된다.

### 5-5) 생성 산출물 요약

| 산출물 | 위치 | 설명 |
|---|---|---|
| SQLite DB | `data/reviews.db` | raw/clean/extractions 3개 테이블 |
| 로그 파일 | `logs/app.log` | INFO/WARNING/ERROR 레벨 기록 |
| 차트 3종 | `output/charts/*.png` | 감정 분포, 시간별 추이, 별점-감정 분포 |
| 종합 리포트 | `output/reports/*.txt`, `*.md` | 핵심 지표, TOP5 키워드, AI 인사이트, 알림 포함 |
| 내보내기 파일 | `output/export_*.*` | CSV / JSONL / Excel 3종 |

---

## 6. 예외 처리 및 한계점

### 예외 처리

| 상황 | 처리 방식 |
|---|---|
| 필수 필드(리뷰 텍스트) 누락/공백 | `clean` 단계에서 제외, WARNING 로그 기록 |
| 별점 범위 초과, 잘못된 형식 | `NULL` 처리 (리뷰 자체는 유지, 별점은 선택 필드) |
| 날짜 형식 불일치 (`YYYY/MM/DD` 등) | 여러 포맷 자동 인식 후 `YYYY-MM-DD`로 통일, 실패 시 `NULL` |
| 중복 리뷰 | `dedup_key` 기준 `skip`(기본) 또는 `upsert` 처리 |
| AI API 호출 실패 | 해당 건만 스킵, ERROR 로그 기록 후 나머지 계속 진행 |
| AI 응답이 JSON 형식이 아닌 경우 | `response_format={"type": "json_object"}`로 강제, 코드블록 마커 제거 후 파싱 |
| 부정 리뷰 급증 알림 표본 부족 (최소 5건 미만) | 알림 생략, WARNING 로그 기록 |

### 한계점

- **AI 응답의 비결정성**: 동일한 데이터라도 재실행 시 감정 점수·키워드 순위가 소폭 달라질 수 있다.
- **키워드 추출 품질**: 부정 리뷰 키워드 추출 시, 문맥과 분리된 단어가 뽑히는 경우가 있다 (예: "부드러워"가 부정 키워드로 추출된 사례 — 원문은 "너무 부드러워서 세척력이 떨어진다"는 맥락이나, 단어만 추출 시 의미가 왜곡됨). 프롬프트에 "문제의 핵심을 담은 구 단위로 추출"하도록 지시를 보강하면 개선 가능하다.
- **부정 리뷰 급증 알림 기준**: "오늘" 기준이 아니라 데이터 내 최신 리뷰 작성일 기준으로 최근/직전 구간을 계산한다. 과거 샘플 데이터 테스트에는 적합하나, 실서비스에서는 현재 날짜 기준으로 전환이 필요할 수 있다.
- **실시간성 없음**: 제약사항에 따라 정적 PNG 차트와 파일 기반 리포트로만 구성되며, 실시간 웹 대시보드는 지원하지 않는다.

---

## 7. 최종 검증표

PDF 요구사항(필수 13개 항목), 보너스 과제, 제약사항 준수 여부를 아래와 같이 최종 검증하였다.

![최종 검증표](images/verification_table.png)

전체 파이프라인은 DB를 초기화한 깨끗한 상태에서 `import → clean → analyze → extract → list/show/stats → dashboard → export` 9개 명령어를 순서대로 재실행하여 재현성을 확인하였으며, 오류 없이 끝까지 정상 동작함을 검증하였다.

---

## 8. 팀원 소감 (가나다순)

### 남병광

테스트와 검수를 진행하면서 기능 구현뿐만 아니라 요구사항과 전체 데이터 흐름, 실행 결과를 함께 확인하는 과정이 중요하다는 점을 배웠습니다. 향후 실무 적용 시에는 AI API 배치 처리와 예외 상황 테스트를 보완하면 더 안정적이고 확장성 있는 서비스가 될 것 같습니다.

### 이혜경

AI기반 고객 리뷰 감정분석 대시보드에 관심이 있었기 때문에 즐거운 마음으로 팀과제를 했습니다. 팀원들이 소통을 잘해주어서 계획했던 기능구현과 보너스과제까지 빠른 시간 내에 잘 마무리할 수 있어서 감사했고, 이 과제를 통해서 AI를 실무에 활용하는 능력이 이전보다 조금 더 향상된 것 같습니다.

### 황준식

팀 미팅 전부터 각자 설계해 보고 방향을 맞춰간 점, 그리고 프롬프트로 AI를 활용하는 효과적인 방법을 서로 공유하며 배운 점이 좋았습니다. 함께 작업 의논하면서 데이터, 방향을 함께 맞춰가며 완성도를 높여가며 많이 배웠습니다! 오류보다, 시행착오를 통해 배워가는 과정이 중요함을 느낀 팀과제였습니다.

---

## 9. 부록 — 사용 AI 도구 및 프롬프트

- **사용한 AI 도구**: Claude
- **프롬프트**: 저장소에 포함된 `프롬프트.txt` 파일 참고 (KDT AI 분야 스타강사 역할 부여, PDF 분석 → 단계별(STEP-by-STEP) 진행 → 최종 검증표 작성까지의 전체 진행 방식을 정의)
- **AI 모델(리뷰 감정분석/추출)**: OpenAI `gpt-4o-mini` (JSON 강제 응답 모드 사용)
- **개발/검증 환경**: Python 3.10+, Windows PowerShell
- **보너스 과제**: 감정 변화 알림(부정 리뷰 급증 감지) 구현
